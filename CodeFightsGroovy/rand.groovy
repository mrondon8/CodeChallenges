This is a reverse challenge, enjoy!

Input/Output

[time limit] 4000ms (rb)

[input] string entropy

A string consisting of digits, commas and brackets.

Guaranteed constraints:
10 ≤ entropy.length ≤ 30.

[output] integer

[Ruby] Syntax Tips

# Prints help message to the console
# Returns a string
def helloWorld(name)
    print "This prints to the console when you Run Tests"
    return "Hello, " + name
end

T rand(e){    
    " 6 7 56      53 9   5        4       56    "[e.hashCode()%43] as int
    //"  5  3 65   5 9   7   5    566     4 "[e.hashCode()%87%37] as int
}


def rand v
  x,y,e = eval "v = #{v}"
  n=[*(x..y)] - e
  n.reverse! if e.size.odd? 
  n[n.size/2]
end
